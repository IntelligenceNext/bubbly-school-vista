
import React from 'react';
import PageTemplate from '@/components/PageTemplate';

const ManageGroups = () => {
  return (
    <PageTemplate title="Manage Groups" subtitle="Organize exam groups" />
  );
};

export default ManageGroups;
