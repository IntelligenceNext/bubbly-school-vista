
import React from 'react';
import PageTemplate from '@/components/PageTemplate';

const Admins = () => {
  return (
    <PageTemplate title="Admins" subtitle="Manage system administrators" />
  );
};

export default Admins;
