
import React from 'react';
import PageTemplate from '@/components/PageTemplate';

const Settings = () => {
  return (
    <PageTemplate title="School Settings" subtitle="Configure individual school settings" />
  );
};

export default Settings;
