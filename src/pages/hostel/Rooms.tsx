
import React from 'react';
import PageTemplate from '@/components/PageTemplate';

const Rooms = () => {
  return (
    <PageTemplate title="Rooms" subtitle="Manage hostel rooms and allocations" />
  );
};

export default Rooms;
